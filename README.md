# Incident Lens

Client-side incident analytics SPA. CSV in, charts out.

Data never leaves the browser tab.

First paint auto-loads Small (80 fake incidents).

Samples: Small 80 seed 801; Medium 800 seed 802; Large 4000 seed 4001.

Upload ceiling 8000 rows inclusive.

Hours are America/New_York.

Use the package scripts named dev and build after installing dependencies.
This host is not an approved GE processing environment. Hobby is fine for fake data.
Wrong control plane for real SN exports. Uploaded rows are not persisted.
